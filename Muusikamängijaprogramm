import tkinter as tk
import pygame
import os
import random
import time
import threading
from tkinter import filedialog
from tkinter import *

#Kasutatud materjalid: https://www.youtube.com/watch?v=jO6qQDNa2UY
#https://www.pygame.org/docs/ref/mixer.html

pygame.mixer.init()
pygame.init()

class UI:
    def __init__(self):
        self.valmista_põhi()
    # loob GUI akna ja komponendid
    def valmista_põhi(self):
        
        self.root = tk.Tk()
    # tkinteri akna loomine
        self.root.geometry("483x600")#kuldlõige
        self.root.title("Muusika mängija")
        # nupud ning teised UI elemendid
        self.playPause_nupp = tk.Button(self.root, text="⏯", font=("Impact", 18), command=self.playpause)
        self.playPause_nupp.pack(padx=10, pady=10)
        self.laululõpukontrollija = False
        
        self.Fail_nupp = tk.Button(self.root, text="Fail", font=("Arial", 18), command=self.valifail)
        self.Fail_nupp.pack(padx= 10, pady=10)
        self.lauludejärjend = []
        self.hetkelmängivalauluindeks = -1
        
        self.järgminelaul_nupp = tk.Button(self.root, text="⏭", font=("Arial", 18), command=self.järgminelaul)
        self.järgminelaul_nupp.pack(padx = 10, pady=10)
        
        self.shuffle_nupp = tk.Button(self.root, text="🔀", font=("Arial", 18), command=self.shuffle)
        self.shuffle_nupp.pack(padx = 10, pady = 10)
        self.korra_juba_tehtud_shuffle = False
        
        self.volüümslider = tk.Scale(self.root, from_=100, to=0, length=200, command=self.slider_väärtused)
        self.volüümslider.set(100)
        self.volüümslider.pack(padx = 10, pady = 10)
        self.lauluscrollslider = tk.Scale(self.root, from_=0, to = 100, orient=HORIZONTAL, length= 400, command=self.scrollilaulus)
        self.lauluscrollslider.pack(padx = 10, pady = 10)
        
        # sulgemise sündmus
        self.root.protocol("WM_DELETE_WINDOW", self.sulgemine)
        # akna avamine
        self.root.mainloop()
    # play/pause nupu funktsioon
    def playpause(self):
        if pygame.mixer.get_busy() == False:
            # kui muusika ei mängi, alustab laulu mängimist, laulu lõppedes käivitab uue laulu valimise threadi
            self.hetkelmängivalauluindeks += 1
            pygame.mixer.Channel(0).play(pygame.mixer.Sound(self.lauludejärjend[self.hetkelmängivalauluindeks]))
            self.Kanal0tööolek = True
            if not(self.laululõpukontrollija):
                self.lõppenud_laulu_kontrolli_eraldi_thread = threading.Thread(target=self.uuslaulpealelõppu)#Sain abi vennalt, Janar-Keit Jaaksonilt.
                self.lõppenud_laulu_kontrolli_eraldi_thread.daemon = True
                self.lõppenud_laulu_kontrolli_eraldi_thread.start()
                self.laululõpukontrollija = True
        
        elif self.Kanal0tööolek:
            pygame.mixer.Channel(0).pause()
            self.Kanal0tööolek = False
        else:
            pygame.mixer.Channel(0).unpause()
            self.Kanal0tööolek = True
     # otsib uue laulu ning mängib kui eelmine läbi saab      
    def uuslaulpealelõppu(self):
        while True:
            if not(pygame.mixer.get_busy()):
                pygame.mixer.Channel(0).stop()
                self.hetkelmängivalauluindeks += 1
                pygame.mixer.Channel(0).play(pygame.mixer.Sound(self.lauludejärjend[self.hetkelmängivalauluindeks]))
                self.Kanal0tööolek = True
                time.sleep(1)
      # võimaldab valida muusikafaili            
    def valifail(self):
        # avab dialoogi, et kasutaja saaks faili valida
        self.failinimi = filedialog.askopenfilenames(title="Vailge helifail mida mängida", initialdir=os.path.expanduser('~/music'), filetypes=[('Audio Files', '*.flac *.mp3 *.wav *.aac *.ogg *.wma *opus *.alac *.mid')])
        if self.failinimi == None:
            pass
        else:
            # lisab valitud faili järjekorda
            for i in self.failinimi: #https://www.geeksforgeeks.org/how-to-add-music-playlist-in-pygame/
                self.lauludejärjend.append(i)
                print(self.lauludejärjend)#See on siin ainult testimiseks
    
    # mängib järgmise laulu järjekorras
    def järgminelaul(self):
        if len(self.lauludejärjend) <= self.hetkelmängivalauluindeks + 1:
            pass
        else:
            # lõpetab praeguse laulu mängimise ning alustab järgmise laulu mängimist
            pygame.mixer.Channel(0).stop()
            self.hetkelmängivalauluindeks += 1
            pygame.mixer.Channel(0).play(pygame.mixer.Sound(self.lauludejärjend[self.hetkelmängivalauluindeks]))
            self.Kanal0tööolek = True
     # shuffle ehk laulude järjekorra segamine
    def shuffle(self):
        # kontrollib kas shuffle on juba tehtud
        if self.korra_juba_tehtud_shuffle == False:
            # esimesel korral salvestab originaaljärjekorra ja segab laulud
            self.originaaljärjend = self.lauludejärjend.copy()
            random.shuffle(self.lauludejärjend)
            self.korra_juba_tehtud_shuffle = True
            self.shuffletöömääraja = True
       # kui shuffle on aktiivne, taastab originaaljärjekorra
        elif self.shuffletöömääraja:
            self.lauludejärjend = self.originaaljärjend
       # ülejäänud puhkudel segab laulud uuesti
        else:
            random.shuffle(self.lauludejärjend)
    
    # volüümi muutmise funktsioon
    def slider_väärtused(self, väärtus):
        sliderväärtus = väärtus
        pygame.mixer.Channel(0).set_volume(float(väärtus) / 100.0)
     
     # laulu kerimise funktsioon (tühi)
    def scrollilaulus(self):
        pass
    
    #rakenduse sulgemine
    def sulgemine(self):
        # peatab helisüsteemi ja sulgeb akna
        pygame.mixer.quit() #https://www.pygame.org/docs/ref/mixer.html
        self.root.destroy()        

UI()
